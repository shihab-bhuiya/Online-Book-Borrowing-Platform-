import React from 'react';

const Loader = () => {
    return (
        <div>
              <span className="loading loading-spinner text-info"></span>
        </div>
    );
};

export default Loader;